# 为什么这个仓库暂时不含插件源码

`routea_warp.addon64` 的源码是一个约 1.3 万行的 C++ 文件。它**暂时没有放进本仓库**,原因不是许可,
而是**卫生**:那份源码里夹着大量开发期的取证注释,包括

* 对**其它 mod**(竞品)实现方式的逆向笔记与文件路径;
* 本机绝对路径、内部实验代号、失败的尝试记录。

这些内容对使用者没有价值,其中的第三方逆向笔记也不应该随发布传播。把它整理成"可公开的源码"
是一项**独立工作**(逐段删改 + 重新验证),没有被塞进 rc1。

`release/` 里的所有产物都是**可验证的**:`BUILD_INFO.txt` 记录插件与钩子的 SHA256,
`rvamap`/`lookup_rva` 可在崩溃时把地址解成函数名(需要与构建同时产生的 `.map`)。

**计划**(未完成):清理出 `src/` 目录,包含 `routea_addon.cpp`(去掉第三方逆向段落)、
`minhook/`(BSD-2,附许可)、构建脚本 `build_release.cmd`,并在 CI 里做"可复现构建"验证。
在此之前,发布以**二进制 + 文档**形式进行。

## 开发者需要源码时

在本机开发树里:`_re/demo_re/harness/routea_addon/`(插件)与 `_re/demo_re/harness/routea/`(引擎侧 ASI)。
两者都不属于发布包。
