# 变更记录(CHANGELOG)

版本号见 `VERSION`;每个发行包内附 `BUILD_INFO.txt`(插件哈希 + 源码哈希 + 构建时间)。

## 0.9.0-rc1 —— 2026-09-25

首个"可交付候选"版本。核心能力:**由 NVIDIA latewarp 内核(NGX feature 15)产生可见的一帧位移**,
不含任何自写的重投影 / 运动矢量 / 深度数学(这是本项目的红线)。

### 功能
* 通过 ReShade 面板(中文标签)控制:**总开关、UI 保护阈值、深度截断、截断扩张、深度反向、显示遮罩**。
* 逐像素 UI 遮罩(高通残差 + 自校准 LUT)+ 去 UI 帧,减轻 HUD 被扭;
* 深度截断保护武器/手部;
* 相机位移来自引擎发布的同帧 delta(通过 Streamline `slSetConstants` 与本作自己的 tag 通道)。

### 稳定性(本版本的重点)
| 修复 | 说明 |
|---|---|
| 交换链身份管理 | `destroy_swapchain` 立刻作废旧后缓冲身份并把交换链代号 +1;`init_swapchain` **先重读新后缓冲**再重建 → 所有资源按新尺寸构建 |
| 内核调用硬门 | 内核心求值/拷贝前检查"本链是否为本代构建 + 后缓冲是否活着",不满足就跳过该帧 |
| 不再对"游戏自己的帧"发 barrier | 该对象生命周期不归我们,换分辨率后它可能已销毁(D3D12Core+0x7AF54 崩溃的直接原因) |
| 延迟销毁栅栏 | `GravePush` 只在该栅栏**确认通过**后释放;仍在飞就"停车"等待,宁可不释放也绝不 GPU 侧 use-after-free |
| 中间纹理复用 | 修掉"每帧新分配 33 MB 纹理"的 churn(GPU 挂起 LiveKernelEvent 141 的主因) |
| 换链后彻底静默 | 兜底触发后,连**建造者**一起停(`SdrEnsure` 拒绝重建、`UiMaskEnsure` 不分配、每帧链整条不跑) |
| 启动不再误触发兜底 | 游戏每次启动会自己重建 3–5 次交换链;兜底只在"扭曲已在运行之后又换链"时生效 |

### 新增
* **面板只读状态**:状态(0 未就绪 / 1 扭曲工作中 / 2 已降级需重启 / 3 失败)、当前后缓冲宽高、
  交换链代号、内核已扭曲帧数(持续变大 = 内核真的在扭)。
* `install.ps1`(`-SelfCheck` / `-DryRun` / `-Uninstall` / `-ForceDefaults`)、`cleanup.ps1`(可释放约 1 GB 调试残留)、
  `build_release.cmd`(`/Brepro` 可复现构建 + `/MAP` 符号表,供崩溃离线解码)。

### 已知限制(见 `README_CN.md` 开头)
* **改分辨率后不要直接回存档**:请先回主菜单再读档,或开游戏前就把分辨率设好。
  直接回存档会在数秒内闪退,原因在游戏/DXGI 侧(插件在换链后已完全静默仍复现)。
* 游戏内改分辨率后本局扭曲会**自动停用**(保护措施),重启游戏恢复。
* HUD **平坦区域**(血条填充、面板底色)仍可能被扭;轮廓与文字受保护。
* 位移为**一帧**位移(使用引擎发布的一帧 delta),当前实现会有一帧延迟观感。
* 与帧生成(DLSS-G)无关,也不依赖它。

---

## 变更:路径选项已移除(2026-09-25,用户决定)

`Route A: which path`(UI first / Light first)这个组合框**已从项目里删除**:

* 插件现在**只有一条路径** —— LDR 扭曲(改扭显示就绪的帧,再拷进后缓冲),也就是实测通过的那条;
* `routea_switch.fx` 里只剩**一个**控件:总开关;
* 原因:UI first 依赖"游戏把自有帧交给内核"(原地改写)这条通道,而当前游戏/驱动不再提供它
  (`Latewarp.Output ret=0xFFFFFFFF`、`GAMETEX bound=0`),留下这个选项只会让人选了之后什么都看不到;
* 代码里那条分支现在永远不会被置位(`g_uiFirstPath` 恒为 0),不存在两条路同时生效的可能。

延迟说明(如实):这条路径的源帧是**上一帧的显示就绪拷贝**,所以画面比输入**晚一帧**;
亮部保留(HDR 不被钳制),HUD 会跟着一起被扭(面板块 [1] 阈值可调保护程度)。
